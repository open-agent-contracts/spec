# Compatibility Registry

Staging stub for compatibility registry rows and indexes.

Future slices will add version-pinned registry entries with explicit covered
fields, gaps, sharp edges, verification dates, and re-check dates.

This stub belongs to `Open Agent Contracts` staging only and is not a
published portal page.

## Placeholders Used

- `Open Agent Contracts` - FEATURE-007 plan section 4.1 P-1. Staging-only
  project/product name placeholder. Must be replaced before portal-repo
  transcription or publication.
